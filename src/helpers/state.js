const canvas = document.querySelector('canvas');
const ctx = canvas.getContext('2d');

const arrayEnemies = [],
    arrayProjectiles = [];

export {
    canvas,
    ctx,
    arrayEnemies,
    arrayProjectiles,
}